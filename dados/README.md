# Diretório de Dados

Coloque seus arquivos CSV de notas de corretagem (padrão Santander) neste diretório.
O aplicativo `performance_app.py` fará a leitura automática e os importará para o banco de dados
ao iniciar, evitando perda de dados em ambientes efêmeros.
